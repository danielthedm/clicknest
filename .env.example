# ClickNest environment variables
# Copy this file to .env and fill in your values.

# GitHub OAuth (optional — enables "Sign in with GitHub" on the settings page)
# Create an OAuth App at https://github.com/settings/developers
# Set the callback URL to: http://your-host/api/v1/auth/github/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Encryption key for API keys and tokens stored in the database (optional)
# If unset, a key is auto-generated and saved to data/.encryption_key
# To set manually: generate with `openssl rand -hex 32`
CLICKNEST_ENCRYPTION_KEY=
