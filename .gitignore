# Build artifacts
clicknest
/data/
/tmp/

# Embedded build copies (generated by make embed)
cmd/clicknest/web_build/
cmd/clicknest/sdk_dist/

# Node
node_modules/
sdk/dist/
web/build/
web/.svelte-kit/

# Fly.io — copy fly.toml.example → fly.toml and fill in your app name
fly.toml

# Safety nets — in case -data flag points somewhere other than ./data
*.duckdb
*.duckdb.wal
.encryption_key
*.env
.env

# Claude Code
.claude/
CLAUDE.md

# OS
.DS_Store
*.swp
